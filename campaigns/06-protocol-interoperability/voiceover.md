Genesis Mesh is not tied to one runtime or one language.

The protocol surface is becoming stable across Python, TypeScript, Go, and .NET.

When multiple implementations verify the same signed trust artifacts, Genesis Mesh becomes a protocol, not just a codebase.
