Genesis Mesh trust primitives are exposed through a stable Trust API and language SDKs.

Developers can verify agreements, boundary decisions, evidence, disclosure proofs, consensus proofs, and data-usage policies without embedding protocol internals.

That is how portable trust becomes usable from real software.
